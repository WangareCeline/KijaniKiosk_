{
  "name": "kijanikiosk-payments",
  "version": "1.0.0",
  "description": "KijaniKiosk payments service - CI pipeline demo",
  "main": "src/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "build": "mkdir -p dist && cp -r nonexistent-folder/* dist/",
    "test": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.4"
  },
  "jest-junit": {
    "outputDirectory": "reports/junit"
  },
  "jest": {
    "testPathIgnorePatterns": ["/node_modules/", "/dist/"]
  }
}
